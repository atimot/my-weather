run:
  timeout: 10m

linters: 
  disable-all: true 
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosimple
    - misspell

issues:
  exclude-use-default: false

linters-settings: 
  govet:
    enable-all: true
